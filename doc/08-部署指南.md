# 🚀 部署指南

本文档介绍如何将 MDtranslator 部署到生产环境。

## 目录

- [部署方式](#部署方式)
- [本地部署](#本地部署)
- [Docker 部署](#docker-部署)
- [云服务部署](#云服务部署)
- [反向代理配置](#反向代理配置)
- [环境配置](#环境配置)
- [监控与日志](#监控与日志)
- [故障排除](#故障排除)

---

## 部署方式

| 方式 | 适用场景 | 复杂度 |
|:---|:---|:---|
| 本地部署 | 开发、测试 | ⭐ |
| Docker | 单机生产 | ⭐⭐ |
| Docker Compose | 完整生产环境 | ⭐⭐⭐ |
| Kubernetes | 大规模生产 | ⭐⭐⭐⭐ |

---

## 本地部署

### Windows (PowerShell)

使用提供的启动脚本：

```powershell
# 启动
.\start.ps1

# 停止 (Ctrl+C 或运行)
.\stop.ps1
```

### Linux/macOS (Bash)

```bash
# 启动
./start.sh

# 停止
./stop.sh
```

### 手动启动

**后端：**

```bash
cd backend
conda activate mdtranslator  # 或使用 venv
uvicorn main:app --host 0.0.0.0 --port 8000
```

**前端：**

```bash
cd src
npm run build
npm start
```

---

## Docker 部署

### Dockerfile (后端)

```dockerfile
# backend/Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY . .

# 创建数据目录
RUN mkdir -p /app/data

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### Dockerfile (前端)

```dockerfile
# src/Dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm ci

# 构建
COPY . .
RUN npm run build

# 生产镜像
FROM node:20-alpine AS runner

WORKDIR /app

ENV NODE_ENV=production

# 复制构建产物
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public

EXPOSE 3000

CMD ["node", "server.js"]
```

### Docker Compose

```yaml
# docker-compose.yml
version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    environment:
      - QWEN_API_KEY=${QWEN_API_KEY}
      - QWEN_API_URL=${QWEN_API_URL}
      - QWEN_MODEL_NAME=${QWEN_MODEL_NAME}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./src
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_WS_HOST=backend:8000
    depends_on:
      - backend
    restart: unless-stopped

networks:
  default:
    name: mdtranslator-network
```

### 启动 Docker Compose

```bash
# 构建并启动
docker-compose up -d --build

# 查看日志
docker-compose logs -f

# 停止
docker-compose down

# 停止并删除数据
docker-compose down -v
```

---

## 云服务部署

### Vercel (前端)

1. Fork 项目到 GitHub
2. 在 Vercel 导入项目
3. 设置环境变量：

```
NEXT_PUBLIC_WS_HOST=your-backend-domain:8000
```

4. 部署

### Railway (后端)

1. 创建新项目
2. 连接 GitHub 仓库
3. 设置根目录为 `backend`
4. 添加环境变量
5. 部署

### 阿里云 ECS

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 克隆项目
git clone https://github.com/your-repo/MDtranslator.git
cd MDtranslator

# 配置环境变量
cp .env.example .env
vim .env

# 启动
docker-compose up -d
```

---

## 反向代理配置

### Nginx

```nginx
# /etc/nginx/sites-available/mdtranslator

upstream backend {
    server 127.0.0.1:8000;
}

upstream frontend {
    server 127.0.0.1:3000;
}

server {
    listen 80;
    server_name your-domain.com;

    # 前端
    location / {
        proxy_pass http://frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # API 代理
    location /api/ {
        proxy_pass http://backend/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket 代理
    location /ws/ {
        proxy_pass http://backend/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400;
    }
}
```

### Caddy

```caddyfile
# Caddyfile

your-domain.com {
    # 前端
    reverse_proxy localhost:3000

    # API
    handle /api/* {
        reverse_proxy localhost:8000
    }

    # WebSocket
    handle /ws/* {
        reverse_proxy localhost:8000
    }
}
```

### HTTPS 配置

使用 Let's Encrypt 免费证书：

```bash
# Nginx + Certbot
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com

# Caddy 自动处理 HTTPS
```

---

## 环境配置

### 生产环境变量

```env
# .env.production

# LLM 配置
QWEN_API_KEY=your_production_api_key
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL_NAME=qwen-turbo

# 应用配置
NODE_ENV=production
NEXT_PUBLIC_WS_HOST=your-domain.com

# 数据库（如果使用外部数据库）
# DATABASE_URL=postgresql://user:pass@host:5432/mdtranslator
```

### 安全配置

```python
# backend/main.py - 生产环境 CORS 配置

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://your-domain.com",
        "https://www.your-domain.com"
    ],
    allow_credentials=True,
    allow_methods=["GET", "POST", "DELETE"],
    allow_headers=["*"],
)
```

### 性能优化

```python
# backend/main.py - 生产环境配置

# 增加并发限制
CONCURRENCY_LIMIT = 10

# 使用 gunicorn + uvicorn workers
# gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker
```

```javascript
// src/next.config.mjs - 生产环境配置

const nextConfig = {
  output: 'standalone',  // 优化 Docker 部署
  compress: true,        // 启用压缩
  poweredByHeader: false,  // 移除 X-Powered-By
};
```

---

## 监控与日志

### 日志配置

**后端日志：**

```python
# backend/main.py

import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('logs/app.log'),
        logging.StreamHandler()
    ]
)

logger = logging.getLogger(__name__)
```

### Docker 日志

```bash
# 查看实时日志
docker-compose logs -f backend
docker-compose logs -f frontend

# 查看最近 100 行
docker-compose logs --tail=100 backend
```

### 健康检查端点

```python
# backend/main.py

@app.get("/health")
async def health_check():
    return {
        "status": "healthy",
        "timestamp": datetime.now().isoformat(),
        "database": "connected"  # 可添加实际检查
    }
```

### 监控建议

| 工具 | 用途 |
|:---|:---|
| Prometheus | 指标收集 |
| Grafana | 可视化仪表板 |
| ELK Stack | 日志聚合分析 |
| Uptime Kuma | 可用性监控 |

---

## 故障排除

### 常见问题

**1. 无法连接到后端**

```bash
# 检查后端是否运行
curl http://localhost:8000/

# 检查端口是否被占用
netstat -tlnp | grep 8000

# 检查防火墙
sudo ufw status
```

**2. WebSocket 连接失败**

```nginx
# 确保 Nginx 配置了 WebSocket 代理
location /ws/ {
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_read_timeout 86400;  # 增加超时时间
}
```

**3. 数据库错误**

```bash
# 检查数据库文件权限
ls -la backend/data/

# 手动测试数据库
sqlite3 backend/data/mdtranslator.db
.tables
```

**4. API 密钥无效**

```bash
# 检查环境变量
echo $QWEN_API_KEY

# 测试 API
curl https://dashscope.aliyuncs.com/compatible-mode/v1/models \
  -H "Authorization: Bearer $QWEN_API_KEY"
```

**5. Docker 内存不足**

```yaml
# docker-compose.yml - 添加资源限制
services:
  backend:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
```

### 日志排查

```bash
# 后端错误日志
docker-compose logs backend 2>&1 | grep -i error

# 前端构建错误
docker-compose logs frontend 2>&1 | grep -i error

# 实时监控
docker-compose logs -f --tail=50
```

---

## 备份与恢复

### 备份数据库

```bash
# 备份
cp backend/data/mdtranslator.db backup/mdtranslator_$(date +%Y%m%d).db

# 自动备份脚本
#!/bin/bash
BACKUP_DIR=/path/to/backup
DB_PATH=/path/to/backend/data/mdtranslator.db
DATE=$(date +%Y%m%d_%H%M%S)

cp $DB_PATH $BACKUP_DIR/mdtranslator_$DATE.db

# 保留最近 7 天的备份
find $BACKUP_DIR -name "mdtranslator_*.db" -mtime +7 -delete
```

### 恢复数据

```bash
# 停止服务
docker-compose down

# 恢复数据库
cp backup/mdtranslator_20240115.db backend/data/mdtranslator.db

# 重启服务
docker-compose up -d
```

---

## 升级指南

### 更新代码

```bash
# 拉取最新代码
git pull origin main

# 重新构建 Docker 镜像
docker-compose build

# 重启服务
docker-compose up -d
```

### 数据库迁移

如果有数据库结构变更：

```bash
# 备份当前数据
cp backend/data/mdtranslator.db backup/

# 运行迁移（如果有迁移脚本）
python backend/migrate.py

# 或者让应用自动创建新表
# 重启后端会自动执行 _ensure_initialized()
```

---

## 下一步

- 📖 [快速入门](./01-快速入门.md) - 回顾基础
- 🛠️ [二次开发](./07-二次开发.md) - 扩展功能
- 📡 [API 参考](./06-API参考.md) - 完整 API 文档
